<h1>{{ title }}</h1>

<a href="/posts/add">Add Post</a>
<hr>
@if(old('notification'))
    <div>
        {{old('notification')}}
    </div>

  
@endif
<ul>
    @each(post in posts)
        <li><a href="/posts/{{post.id}}">
             {{ post.title }}</a></li>
    @endeach
</ul>
